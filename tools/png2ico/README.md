# png2ico
png to ico

png转ico

## 使用方法：
1. 运行：
```
./png2ico.exe -i cs.png -o cs.ico -s "16,32,48,64,128,256"
```

## 参数：
- -i string    
      PNG 文件路径（必填）

- -o string    
      输出 ICO 文件路径（默认与输入同名）

- -s string    
      ICO尺寸，用逗号分隔 (default "16,32,48,64,128,256")