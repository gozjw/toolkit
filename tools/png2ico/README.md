# png2ico
png to ico

png转ico

## 使用方法：
1. 运行：
```
./png2ico.exe -i cs.png -s "16,32,48,64,128,256"
```

## 参数：
- -i string    
      输入 PNG 文件路径（必填）

- -s string    
      ICO尺寸，用逗号分隔 (default "16,32,48,64,128,256")